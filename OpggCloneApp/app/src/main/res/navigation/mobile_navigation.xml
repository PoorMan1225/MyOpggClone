<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/search_dest">

    <fragment
        android:id="@+id/search_dest"
        android:name="com.rjhwork.mycompany.opggcloneapp.presentation.search.SearchFragment"
        android:label="검색"
        tools:layout="@layout/fragment_search"
        >

        <action
            android:id="@+id/action_search_dest_to_ranking_dest"
            app:destination="@id/ranking_dest" />
    </fragment>

    <fragment
        android:id="@+id/ranking_dest"
        android:name="com.rjhwork.mycompany.opggcloneapp.presentation.ranking.RankingFragment"
        android:label="랭킹">
    </fragment>
</navigation>